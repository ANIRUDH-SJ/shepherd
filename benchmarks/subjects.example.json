{
  "schemaVersion": 1,
  "display": ":0",
  "subjects": [
    {
      "id": "shepherd-unpacked",
      "label": "Shepherd unpacked production build",
      "mode": "shepherd",
      "command": "/absolute/path/to/shepherd/release/linux-unpacked/shepherd",
      "args": [],
      "versionCommand": "/usr/bin/git",
      "versionArgs": ["-C", "/absolute/path/to/shepherd", "rev-parse", "HEAD"],
      "source": "local production build; replace with the exact commit"
    },
    {
      "id": "gnome-terminal",
      "label": "GNOME Terminal",
      "mode": "terminal",
      "command": "/usr/bin/gnome-terminal",
      "args": ["--wait", "--geometry=110x38", "--", "{shell}"],
      "versionCommand": "/usr/bin/gnome-terminal",
      "versionArgs": ["--version"],
      "source": "system package; replace with the exact package version",
      "relatedCommands": ["gnome-terminal-"]
    },
    {
      "id": "kitty",
      "label": "Kitty",
      "mode": "terminal",
      "command": "/absolute/path/to/kitty",
      "args": ["--config", "NONE", "--override", "font_family=monospace", "{shell}"],
      "versionCommand": "/absolute/path/to/kitty",
      "versionArgs": ["--version"],
      "source": "official release; replace with the exact version and checksum"
    }
  ]
}
